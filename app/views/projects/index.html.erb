<%= link_to 'Create A New Project', new_project_path %>

<% @projects.each do |project| %>
	<p>
		<%= link_to "#{project.title}", project_path(project.id)%>
		<img src="<%= project.picture_url %>"/> 
		<!-- picture, title, date, funding(goal and progress) -->
	
	</p>

<% end %>